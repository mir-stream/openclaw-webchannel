{
  "name": "@openclaw/webchannel-saas",
  "version": "0.0.0",
  "description": "Headless SaaS trust chain core + reference harness for WebChannel NATS relay",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "setup-trust-chain": "node ./dist/reference/setup-trust-chain.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "tsx": "^4.22.4"
  },
  "dependencies": {
    "@nats-io/jwt": "0.0.10-5",
    "@nats-io/nkeys": "2.0.3"
  }
}
